# 2026-06-19 Ubuntu

## 今日推进

- 跑完 `old` 视觉模型三种 state 的视觉 student PPO：`xd`、`feature`、`shared`。
- 完成三组模型的离线评估，对比 `offline_val.csv`、best checkpoint 和 best 轨迹。
- 确认 `old_shared_student` 表现最好，best success 达到 1.0，last success 仍有 0.96875。
- 确认 `old_xd_student` 中期可用但后期退化，best success 为 0.84375，主要失败是未对准图像中心就停住。
- 确认 `old_feature_student` 基本失败，能到 stop 附近但不输出 stop 动作。
- 将三组结果保存到 `approved_models/2026_06_22_student_vision_old_state_compare/`。
- approved 结果只保存 best、last、model_1000、配置、日志、离线评估和 best 轨迹，未保存接近 1.5G 的逐 update checkpoint。
- 整理训练/评估入口命名，`train/` 与 `val/` 目录分别放训练和离线评估脚本。
- 继续排查 VS Code / inotify watch 占用问题，确认 6 月 16、17 已有 watch limit 记录，6 月 19 晚间问题明显恶化。

## 当前判断

- 冻结 encoder 的视觉 student 第一版中，`old_shared` 比 `old_xd` 和 `old_feature` 更适合作为 PPO state。
- `old_feature` 的失败点不是接近能力，而是 stop 动作学习失败；后续不应优先继续投入。
- `old_xd` 直接回归输出可用但稳定性差，后期可能学到偏离中心的停止策略。
- 当前可作为阶段性结果保留的是 `approved_models/2026_06_22_student_vision_old_state_compare/old_shared/best_offline.pt`。
- 后续如果继续视觉 student，优先围绕 `old_shared` 做 teacher loss、reward、stop 机制或表征训练对照。
