# ACTIVE_PLAN — 当前执行计划

> 只放当前里程碑。历史见 `RESEARCH_LOG.md`,长期旧 TODO 见 `TODO.md`(大部分已过时)。更新于 2026-09-25。

## 当前里程碑

**支撑 SI2026 论文写作(2 物体环境)。** 论文由用户与本地 CC 在本地写(`si2026_draft_zh.typ`,不进 git);
服务器端(本仓库的研究会话)负责:回答本地 CC 的数据/口径问题,并保证论文里每个数字都能追溯到仓库产物。

## 为什么重要

这是当前唯一在推进的产出。数字口径若不统一(检查点、λ、可提取性定义),审稿时会被抓住。

## 当前任务

1. **等待并回答本地 CC 的问题。** 渠道:本地 CC 在 `summary/` 下提交问题文件并 push(先例:
   `2026_09_24_cc_questions_for_paper.md`),服务器端在同一文件末尾回复、只提交该文件。
   - 验证方法:每条回复都引用仓库内的产物路径;只读核对,不跑新实验。
2. **(可选,需用户同意)补三格缺失的检查点评估**:R 红 s1、sup 红 s1、Ax 蓝 u240(CURRENT_STATE U4)。
   - 需要启动 Isaac,**先问用户**。缺的检查点可能高于现有最大值,所以这三格当前的数字只是**下界**;
     补齐后若有变化,要同步更新 `ppo_table.csv` 与 `CURRENT_STATE.md`。
3. **(可选,需用户决定)把 S3、S4 的临时计算保存成脚本**,使 LayerNorm γ/β 与 V 目标斜率可复现。纯 CPU。

## 完成判据

- 用户确认论文原稿提交;或本地 CC 的所有问题都已回复且无待确认项。
- 论文中引用的数字全部能在 `summary/results/` 或 `ppo_table.csv` 找到,口径与 `DECISIONS.md` D-003、D-008 一致。

## 不要做

- 不要把 4 物体环境的结论带进论文(D-006)。
- 不要新增监督臂、改 teacher 配置、提 Q/advantage(D-002、D-004)。
- 不要未经同意启动 Isaac(D-010)。
- 不要处理 mutmuas 相关事务(D-011,由秘书会话负责)。
- 不要重写历史文档的正文;需要更正时在文件头加注,并更新 `CURRENT_STATE.md`。

## 延后

- 编码器多种子重复(U1)、不同尺寸干扰物(U2)、池化方式对照以检验 H4、LayerNorm 干预以检验 H1。
- 4 物体环境海报材料整理。
- `TODO.md` 中"未来线:避障/搜寻/找回"的设计(含硬约束"信号只编码在哪、不编码是谁")。
- `summary/results/job_logs/` 里的流水线脚本只作记录;若要重跑,先改成仓库内正式脚本。
