# 2026-06-17 Ubuntu

## 今日推进

- 完成 AGENTS / shared rules 的规则分层，明确 Codex 与共享规则边界。
- 搭建 IsaacLab 任务流程、SB3 teacher PPO、自写 PPO student、视觉 student 训练入口。
- 明确找球任务流程：search、看到球、approach、进入 stop 区、连续 stop success。
- 完成 teacher / student 训练、策略预览、轨迹记录和 approved 模型保存流程。
- 对比 teacher loss 与 no-teacher 的 student PPO，初步观察到 teacher loss 能加快收敛。
- 搭建 CV 训练四模型流程：`resnet`、`mobile`、`old`、`old-mobile`，并支持固定 seed 的 val 对比。
- 确认 old 结构整体精度较好，resnet 通常优于 mobile。
- 将采集标签中的 `dist` 改为相机前向轴方向距离，并保留看不见球时 `x=-1, dist=0` 的设计。
- 测试 Isaac 视觉训练性能，确认主要瓶颈在相机渲染和 encoder 前向。
- 对比相机渲染模式后，暂定视觉训练使用 `balanced + DLSS performance(dlss_mode=0)`。
- 新增一键视觉流程脚本：采集 50000 张数据、训练四个 CV 模型、接入 `old + xd` 跑视觉 student。
- 清理临时性能细分日志，只保留原本需要的 fps 输出。

## 当前判断

- `approved_models/` 中的 teacher / student 对比模型可以上传 Git；`models/` 仍作为本地训练现场输出，不上传。
- 视觉 student 第一版先用冻结 encoder，把 CV 输出或中间特征作为 PPO state。
- 后续真正接入视觉 student 时，需要继续保证 `student_obs` 和 `priv_obs` 分离：teacher 只能吃特权观测，不能吃视觉特征。
- 视觉训练速度目前优先从渲染配置、env 数和 backbone 选择上优化，不优先排查 CSV、保存或普通 Python 判断。
