# 2026-06-26 Ubuntu

## 今日推进

- 继续整理 teacher / student / MDP student 的训练与离线评估流程，确认 teacher 的离线评估支持 `--start`、`--stride`，且 `start>0` 时追加写入 CSV。
- 跑并分析 teacher `update_88` 的全 env 离线轨迹：`506/512` 成功，6 条失败均为 timeout，无 out/fail。
- 确认 teacher 失败样本集中在初始目标位于相机右侧视野外且距离较远的情况；主要瓶颈是 search 阶段转头较慢，进入 stop 区域太晚。
- 从 teacher checkpoint 读取 `log_std`，确认 teacher std 在约 update 60 降到 0.1，update 76 降到 0.05，之后基本稳定在 0.04 左右。
- 将 `src/sb3_env.py` 改为由 env 统一维护 stop end state：特权观测包含 `[px, dist, end_d, end_x]`，reward、stop 区域和 done 判断使用同一份 end state。
- 将 approach 阶段状态改善 reward 改为固定尺度归一化：`px` 改善除以 `image_width=224`，`dist` 改善除以 `dist_max-fail_near=6`。
- 同步视觉 student 和 MDP student 的策略输入，使视觉/MDP state 显式拼接 `end_obs`；MDP student 中使用 `mdp_policy_obs()` 避免覆盖 base env 的 `policy_obs(x,d)`。
- 检查 rl_games 训练和 preview 逻辑，确认 rl_games 使用独立环境 `rl_games_ball/env.py`，不会自动继承 `src/sb3_env.py` 的 reward 修改。
- 新增周末批量脚本 `scripts/run_weekend_students.sh`，按顺序训练并用 `stride=10` 评估 `old+xd`、`old+shared`、`MDP student`。

## 当前判断

- teacher 当前失败不是接近或 stop 逻辑崩坏，而是边界不可见目标 search 过慢；后续全随机找球阶段再专门处理 search reward / search policy。
- `end_d/end_x` 是任务目标，不应混入 MDP latent 本体；当前更合理的是在 policy 输入处显式拼接，后续如目标信息太弱可加小型 goal encoder。
- `224,K_X=1` 与 `112,K_X=0.5` 等价；当前先使用 `224` 固定尺度，后续只调 `K_X/K_D`。
- 周末实验先不覆盖 `approved_models/`，等跑完后再统一和旧版本对比并挑选需要保留的模型。

## 后续

- 跑完 `scripts/run_weekend_students.sh` 后，对比三组模型的 `offline_val.csv` success 曲线、best checkpoint 和 `traj_best_offline.csv` 失败模式。
- 若视觉 student 仍长期 timeout，优先区分是 search 不足、stop 动作不进死区，还是 latent/state 表达不足。
