# 接手文档 2026-09-23(换 CC 版本前保存)

> **[2026-09-25 会话迁移注]** 已被 `summary/HANDOFF.md` 取代;其中'主张降档、4 物体结论'仍有效,但 4 物体结论只进海报(`DECISIONS.md` D-006)。

## 机器状态

两卡全空、无残留进程、无在跑的链、git 干净(HEAD `ee91c4d`)。**可以直接退出,不会丢东西。**

## 这条线在研究什么(三层框架,用户反复强调,别讲错)

1. 表征能从行为学出来 —— **别人做过,只当背景**
2. 学到的对行为有用 —— **别人做过,只当背景**
3. **表征里面是什么 + 各个行为信号对表征学习的影响** ← **这才是贡献**

约束:**无人工标注监督、无特权信息**(student 端)。teacher 可以吃一切,当作人工筛选的 oracle。
sup / init 只是基线与参照,**不是研究对象**;A/V/R 才是。不提 Q / advantage。

## 主张的档位(2026-09-18 定,**降档**)

只写第二档:

> **在多条可用线索中,表征选了一条当判据 —— 而且选的不是 teacher 用的那条。**

落点句:**行为信号决定表征保留多少(可预测、可控);但"用哪条线索"是表征自己选的。**

三档(自主涌现)撑不住:init 的 8 类颜色解码就有 60.2%,颜色信息先验里本来就有。
详见 `TODO.md` 的「论文主张的档位」一节。

## 已经立住的结论(全部有表)

**2 物体环境**(`2026_09_18_final_results.md`):
- 主表 best-of-last-6:A 红 95.8 / 蓝 10.4;R 红 97.9 / 蓝 83.3;V 79.2 / 78.6;init 76.6 / 81.2;sup 16.7 / 25.0
- **R 97.9 > A 95.8 → 擦除什么也没买到**
- 机制律:目标在随机先验上的**可提取性** → proj ΔW → 干扰残留,**六臂单调零例外**
  (Ax 0.965/+0.559、R 0.964/+0.560、V 0.901/+0.428、Asym 0.857/+0.177、A 0.710/−0.079、Aw 0.455/−0.116)
- 机制:奖励用 `|bearing|`(镜像不变,随机卷积就有 0.96);`a_w` 反对称(随机卷积只有 0.46)
  → 必须重建 → LayerNorm 定范数下是零和 → 把别的挤出去。**保留是默认,擦除是副作用。**
- Ax/Aw 的**预登记外推两条全中**(§27):Ax 蓝 81.2 ≈ R 的 83.3;Aw 蓝 18.8 ≈ A 的 10.4。
  "动作 vs 奖励"和"目标维数"两个竞争解释同时死。

**4 物体环境**(`2026_09_20_w4_results.md`,24 条 PPO **0 失败**):
- 归一化(/ init 同槽):R 2.10/1.12/0.44/0.63;V 2.07~2.14/0.71~0.76/0.00/0.07~0.09;
  A 2.10/0.37/0.00/0.00;sup 1.97/0.35/0.00/0.02
- **擦除 4/4 零例外**:A 与 R 原任务原始分**完全相同(都 95.3)**,其余三槽 R 全面高于 A
- **"A 丢弃非红物体"是错的** —— 同色的红方也被压到随机的 0.37 倍。**丢的是"非目标"**
- **颜色是门,形状是门内的选择器**(纠正 09-19 的说法):
  base 下蓝球 0.038 ≈ 蓝方 0.041(过不了门);allred 把颜色消掉后球 0.365/0.375 ≫ 方 0.070/0.158。
  **A 学的是「红 ∧ 球」的合取**
- **三档不是两档**:A 擦到底、V 只保同色、R 跨色也保一部分。
  **"V/R 把干扰物当无关项"不成立**(V 双种子四格最大差 0.05,读数是稳的)
- **机制律跨环境外推**:可提取性 / proj 残留 / 红方 PPO / 红方探针 R² / 颜色敏感度**五列同序**
- **"R 保留一切"要收窄**:R 在两个蓝物体上 0.44/0.63,都低于随机编码器
- 机制判决:冻住的"红球方位"探针喂给颜色翻转图,**跟着红球走不跟位置走**(A +0.947 / 原位 −0.930)

## 方法学(会把结论读反的那条)

4 物体环境里四个槽的**内在难度差一倍,且由形状造成**
(init 定位得了方块、定位不了球:R² 0.530/0.488 对 −0.179/−0.071)。
**必须逐槽对 init 归一化。** 2 物体环境红蓝都是球、init 基本对称,才能直接看原始分。

## 固定配置(不再动)

teacher:`--no-lateral --obs-mask diam --score-mode angle`,不带 `--noise`;
复用 `models/rl/teacher_score_g4_diam_angle_s2`(v1=100%、回报 276.2)。
2×2 消融已证空(臂 91–100%、teacher 输入 0–5%、奖励度量 0–3%)。
报数口径:**末 6 个 ckpt 取最好,排除 last**;只排除 t=0 就看不见目标的起点。

## 下一步(等用户定)

`TODO.md` 里有两块待办:
- **未来线:避障 / 搜寻 / 找回**,附一条硬约束:**信号只编"在哪 / 代价多少",绝不编"是谁 / 哪一类"**
  (否则"自发/选择性"那一栏在复杂任务里永远加不了新条目)
- **悬而未决**:MSE 是否逼出过度精确(用户说"先留个疑问",不要自行销掉);
  第二个创新(模糊表征 + 匹配的精准策略)押在未来那条线;**走到那一步要重新 grill 一次**

## 纪律(都栽过,写进记忆了)

- 队列 worker 每次起 Isaac 前 reap 本卡,**队列期间那张卡没有安全窗口**;多轮脚本要**每轮落盘**
- 等待条件绝不用 ps 匹配字符串;杀进程先单独拿 PID 再 kill 数字,绝不 `pkill -f`
- 所有 Isaac 调用带 timeout;CPU 分析用 `CUDA_VISIBLE_DEVICES=""`;一张卡同时只跑一个 Isaac
- **正在跑的 bash 调度脚本不能改**(bash 边读边执行),要加新任务类型只能重启或另挂一条链
